fun fib(n){
    if( n <= 1 ) return n;
    return fib(n-1) + fib(n-2);
}

// IDEAS:
// .shell = "bash";
// @ ... any bash command ...
// @( ... any bash command ... )
// @r( ... any bash command ... )
// => %<variableName> => %{ <expression> }

/*
use jh_scripts as sh;

fn main(){
    sh::config({
        "shell": "bash",
    });

    let result : Vec<String> = sh::run(
        for file in * ; do
            echo $file
        done
    );

    sh::run(
        rm %{ result[0] }
    );
}
*/