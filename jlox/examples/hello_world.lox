var start = clock();

var number = 0;
var cycle  = 1000000000;
for(var i = 0; i < cycle; i = i + 1){
    number = number + 1;
}

var duration = clock() - start;
print "Did " + cycle + " cycles in " + duration + " seconds.";